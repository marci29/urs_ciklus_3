/* 21. Napisati program koji u petlji čita s GPIOA0-GPIOA7 I ispisuje ih na GPIOA8-GPIOA15 na način: ako je pročitan parni broj dodaj mu jedan I ispiši, ako je pročitan neparni samo ga ispiši. 
Za čitanje i ispis potrebno je koristiti scanf() I printf() (redefiniranjem). Inicijalizacija je već napravljena pomoću funkcije init_GPIOA(). */
